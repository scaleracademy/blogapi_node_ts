# Blog API (NodeJS + Typescript)

## Project Setup 

```shell 
npm install -D typescript @types/node ts-node
npm install express
npm install -D @types/express
npm i -s @mikro-orm/core @mikro-orm/sqlite
```